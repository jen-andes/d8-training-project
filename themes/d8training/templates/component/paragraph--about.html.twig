{#
/**
 * @file
 * Theme override to display an 'About' component.
 */
#}

{{ attach_library('d8training/about') }}
{% include '@d8training/about/about.twig' with {
    'kicker': content.field_kicker.0['#context'].value,
    'description': content.field_plain_text.0['#context'].value,
    'images': image_attr(content.field_images),
  } only
%}
